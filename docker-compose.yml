services:
  moisa:
    build: .
    container_name: moisa
    ports:
      - "8080:8080"
    environment:
      - TORRENTIO_BASE=https://torrentio.strem.fun
      - PORT=8080
    restart: unless-stopped
